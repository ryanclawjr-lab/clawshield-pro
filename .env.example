# Required: Secret used to sign fishnet-auth tokens
# Generate a strong random string for production
FISHNET_AUTH_SECRET=your-secret-here

# Optional: Base URL for the application (used in session spectator URLs)
# Defaults to http://localhost:3000 in development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional: Required only for running test-anthropic.sh
# Get your key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
